#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.4.1.0@mocha-phantomjs/bin/mocha-phantomjs" "$@"
  ret=$?
else 
  node  "$basedir/../.4.1.0@mocha-phantomjs/bin/mocha-phantomjs" "$@"
  ret=$?
fi
exit $ret
